<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.simple.apps">

    <application
        android:name=".base.BaseApp"
        android:allowBackup="true"
        android:icon="@drawable/logo_blue"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".test.exam1.DownloadExamActivity" />
        <activity android:name=".test.utils.log.LogExam1Activity" />
        <activity android:name=".webapps.PluginsTest2Activity" />
        <activity android:name=".webapps.service.hardware.SocInfoActivity"></activity>
    </application>

</manifest>